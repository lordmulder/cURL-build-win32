 src/tool_operate.c | 8 ++++++--
 1 file changed, 6 insertions(+), 2 deletions(-)

diff --git a/src/tool_operate.c b/src/tool_operate.c
index 3ad30ca..b6ace22 100644
--- a/src/tool_operate.c
+++ b/src/tool_operate.c
@@ -2241,11 +2241,15 @@ static CURLcode cacertpaths(struct OperationConfig *config)
 #ifdef _WIN32
   if(!config->capath && !config->cacert) {
 #ifdef CURL_CA_SEARCH_SAFE
-    char *cacert = NULL;
-    FILE *cafile = tool_execpath("curl-ca-bundle.crt", &cacert);
+    TCHAR *cacert = NULL;
+    FILE *cafile = tool_execpath(TEXT("curl-ca-bundle.crt"), &cacert);
     if(cafile) {
       curlx_fclose(cafile);
+#ifdef UNICODE
+      config->cacert = curlx_convert_wchar_to_UTF8(cacert);
+#else
       config->cacert = curlx_strdup(cacert);
+#endif
       if(!config->cacert) {
         result = CURLE_OUT_OF_MEMORY;
         goto fail;
